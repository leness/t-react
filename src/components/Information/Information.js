const Information = ({children}) => {
    return (
        <div style={{backgroundColor: 'teal'}}>
            <h1>Information</h1>
            {children}
           
        </div>
    )
}

export default Information